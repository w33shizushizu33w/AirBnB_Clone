<%= provide(:title, "Location") %>
<div class="row mx-4">
    <div class="col-2">
        <%= render 'rooms/menu' %>
    </div>
    <div class="col-10">       
        <div class="card container d-flex justify-content-center mt-4">
            <div class="card-header">
                <h3>Location<h3>
            </div>
            <%= form_for(@room) do |f| %>
            <div class="card-body">
                <div class="form-group mx-4">
                    <%= f.label :Address %>
                    <%= f.text_field :address, class: "form-control" %>
                </div>
            </div>
            <div class="text-center pb-4">
                <%= f.submit "Update Location", class: "btn btn-outline-dark" %>
            </div>
            <% end %>
        </div>       
    </div>
</div>